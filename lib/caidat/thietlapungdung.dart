import 'package:flutter/material.dart';

class ThietLapUngDung extends StatelessWidget {
  const ThietLapUngDung({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Thiết lập ứng dụng'),
      ),
    );
  }
}
